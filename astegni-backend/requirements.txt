# Core packages
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23

# Database driver (psycopg3)
psycopg[binary]>=3.2.3

# Authentication
PyJWT==2.8.0
bcrypt==4.1.2
pyotp==2.9.0
qrcode[pil]==7.4.2

# Form handling and validation
python-multipart==0.0.6
email-validator==2.1.0.post1
python-dotenv==1.0.0

# Pydantic
pydantic==2.4.2

# Cloud storage (Backblaze B2)
b2sdk

# Email and SMS services
aiosmtplib==3.0.1
twilio==9.0.4

# Rate limiting
slowapi==0.1.9

# Encryption
cryptography==41.0.7

# HTTP client for external API calls (Google Translate, etc.)
httpx==0.27.0

# Computer Vision - KYC Identity Verification
opencv-python==4.12.0  # Face detection and liveliness detection (blink, smile, head turn)

# Face Recognition - Production Face Matching
# Note: dlib and face_recognition require build tools to install:
#   - Linux (Ubuntu/Debian): apt install cmake build-essential
#   - Windows: Visual Studio Build Tools + CMake
# Uncomment the lines below for production deployment with real face matching:
dlib>=19.24.0
face-recognition>=1.3.0
# Without these: KYC uses placeholder face matching (82-98% random scores for testing)

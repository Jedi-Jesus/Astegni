# Sessions Panel Architecture - Visual Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          STUDENT PROFILE PAGE                            │
│                     (profile-pages/student-profile.html)                 │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ User clicks "My Sessions"
                                    ↓
        ┌───────────────────────────────────────────────────────┐
        │          PANEL SWITCH EVENT                            │
        │   window.dispatchEvent('panelSwitch', 'sessions')     │
        └───────────────────────────────────────────────────────┘
                                    │
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│              SESSIONS PANEL MANAGER (JavaScript)                         │
│          js/student-profile/sessions-panel-manager.js                    │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  EVENT LISTENER                                                 │    │
│  │  window.addEventListener('panelSwitch', (event) => {           │    │
│  │      if (event.detail.panelName === 'sessions') {              │    │
│  │          loadSessions();        // Load session data           │    │
│  │          loadSessionStats();    // Load statistics             │    │
│  │      }                                                          │    │
│  │  });                                                            │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  STATE VARIABLES                                                │    │
│  │  • allSessionsData = []         (all fetched sessions)         │    │
│  │  • sessionStats = null          (statistics object)            │    │
│  │  • sessionCurrentPage = 1       (pagination)                   │    │
│  │  • currentRoleFilter = 'all'    (active filter)                │    │
│  │  • filteredSessionsCache = []   (search/filter cache)          │    │
│  │  • sessionSortField = null      (sort column)                  │    │
│  │  • sessionSortDirection = 'asc' (sort order)                   │    │
│  └────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                ┌───────────────────┴───────────────────┐
                │                                       │
                ↓                                       ↓
    ┌──────────────────────┐              ┌──────────────────────┐
    │  loadSessions()      │              │  loadSessionStats()  │
    │  (default: student)  │              │                      │
    └──────────────────────┘              └──────────────────────┘
                │                                       │
                ↓                                       ↓
┌───────────────────────────────┐      ┌──────────────────────────────────┐
│ loadSessionsByRole('student') │      │ GET /api/student/my-sessions/    │
└───────────────────────────────┘      │     counts                       │
                │                       └──────────────────────────────────┘
                │                                       │
                ↓                                       ↓
┌─────────────────────────────────────────┐   ┌─────────────────────────┐
│  ROLE FILTER DECISION TREE              │   │  UPDATE STAT CARDS      │
│                                         │   │  #student-session-stat- │
│  if role === 'all':                     │   │  • total                │
│      Fetch from 3 endpoints in parallel │   │  • completed            │
│      ├─ GET /api/tutor/sessions         │   │  • hours                │
│      ├─ GET /api/student/my-sessions    │   │  • upcoming             │
│      └─ GET /api/parent/sessions        │   └─────────────────────────┘
│                                         │
│  else if role === 'student':            │
│      GET /api/student/my-sessions       │
│                                         │
│  else if role === 'tutor':              │
│      GET /api/tutor/sessions            │
│                                         │
│  else if role === 'parent':             │
│      GET /api/parent/sessions           │
└─────────────────────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────┐
│  STORE SESSIONS                         │
│  allSessionsData = fetchedSessions      │
│  filteredSessionsCache = fetchedSessions│
└─────────────────────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────────────────────────────┐
│  displayFilteredSessions(sessions, viewRole)                    │
│                                                                  │
│  1. Determine table headers based on viewRole:                  │
│     ┌───────────────────────────────────────────────────────┐  │
│     │ TUTOR VIEW:   Student | Course | Date | Status | 🔔  │  │
│     │ STUDENT VIEW: Tutor   | Course | Date | Status | 🔔  │  │
│     │ PARENT VIEW:  Child   | Tutor  | Date | Status | 🔔  │  │
│     │ ALL VIEW:     Role    | Name   | Date | Status       │  │
│     └───────────────────────────────────────────────────────┘  │
│                                                                  │
│  2. Apply pagination (10 items per page)                        │
│                                                                  │
│  3. Generate table rows with:                                   │
│     • Status badges (color-coded)                               │
│     • Notification toggle icon                                  │
│     • Alarm toggle icon                                         │
│     • View button                                               │
│                                                                  │
│  4. Render pagination controls (if >10 sessions)                │
│                                                                  │
│  5. Insert into #sessions-table-container                       │
└─────────────────────────────────────────────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────────────────────────────┐
│                    SESSIONS TABLE (HTML)                         │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Tutor Name        Course         Date      Status  🔔  │    │
│  │ ─────────────────────────────────────────────────────  │    │
│  │ John Smith     Mathematics   Jan 30    Scheduled ✓ 🔔 │    │
│  │ Sarah Johnson  Physics       Jan 31    In Progress ✗  │    │
│  │ Mike Davis     Chemistry     Feb 1     Completed ✓ 🔕 │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│              Showing 1-10 of 15 sessions                         │
│              [<] [1] [2] [>]                                     │
└─────────────────────────────────────────────────────────────────┘
```

## User Interaction Flows

### 1. Filter by Role

```
User clicks "As Tutor" button
    │
    ↓
filterSessionsByRole('tutor', event)
    │
    ├─ Update button styles (highlight clicked button)
    │
    ├─ currentRoleFilter = 'tutor'
    │
    ├─ sessionCurrentPage = 1 (reset pagination)
    │
    └─ loadSessionsByRole('tutor')
           │
           ↓
       GET /api/tutor/sessions
           │
           ↓
       Store in allSessionsData
           │
           ↓
       displayFilteredSessions(sessions, 'tutor')
           │
           ↓
       Table shows: Student Name | Course | ...
```

### 2. Search Sessions

```
User types "mathematics" in search bar
    │
    ↓
searchSessions("mathematics")
    │
    ├─ Filter allSessionsData locally (no API call)
    │   ├─ Check tutor_name
    │   ├─ Check course_name ✓ MATCH
    │   ├─ Check topics[]
    │   └─ Check status
    │
    ├─ filteredSessionsCache = matchedSessions
    │
    └─ displayFilteredSessions(matchedSessions, currentRoleFilter)
           │
           ↓
       Shows only sessions with "mathematics" in course name
```

### 3. Toggle Notification

```
User clicks notification icon (🔔)
    │
    ↓
toggleSessionNotification(sessionId=123, enable=true)
    │
    ├─ Validate token exists
    │
    ├─ PATCH /api/student/sessions/123/toggle-notification
    │   Headers: { Authorization: Bearer ${token} }
    │   Body: { notification_enabled: true }
    │
    ├─ If response.ok:
    │   ├─ loadSessionsByRole(currentRoleFilter) // Reload
    │   └─ console.log("✅ Session 123 notification enabled")
    │
    └─ If error:
        └─ alert("Failed to update notification setting")

Result: Icon changes ✗ → ✓ (gray to green)
```

### 4. Sort by Column

```
User clicks "Date & Time" column header
    │
    ↓
sortSessionsByColumn('session_date')
    │
    ├─ Check if same field as before:
    │   └─ Yes: Toggle direction (asc ↔ desc)
    │   └─ No: Set field, direction='asc'
    │
    ├─ Sort allSessionsData in-memory
    │   └─ Special handling for dates (new Date())
    │
    └─ displayFilteredSessions(allSessionsData, currentRoleFilter)
           │
           ↓
       Re-render table with sorted data
```

### 5. Paginate

```
User clicks "Next" button or page number
    │
    ↓
loadFilteredSessionsPage(pageNumber)
    │
    ├─ sessionCurrentPage = pageNumber
    │
    └─ displayFilteredSessions(filteredSessionsCache, currentRoleFilter)
           │
           ├─ Calculate slice: [startIndex, endIndex]
           │   startIndex = (page - 1) * 10
           │   endIndex = startIndex + 10
           │
           ├─ paginatedSessions = sessions.slice(startIndex, endIndex)
           │
           └─ Render only 10 sessions
```

## Data Flow Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        API LAYER                                 │
│                  (FastAPI Backend - Port 8000)                   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Student Endpoints                                        │  │
│  │  • GET  /api/student/my-sessions                         │  │
│  │  • GET  /api/student/my-sessions/counts                  │  │
│  │  • PATCH /api/student/sessions/{id}/toggle-notification │  │
│  │  • PATCH /api/student/sessions/{id}/toggle-alarm        │  │
│  │  • PATCH /api/student/sessions/{id}/toggle-featured     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Multi-Role Endpoints (for role filtering)               │  │
│  │  • GET /api/tutor/sessions                               │  │
│  │  • GET /api/parent/sessions                              │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↕ HTTP/JSON
┌─────────────────────────────────────────────────────────────────┐
│                    JAVASCRIPT LAYER                              │
│          js/student-profile/sessions-panel-manager.js            │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Core Functions                                           │  │
│  │  • loadSessions() - Entry point                          │  │
│  │  • loadSessionsByRole(role) - Fetch by role              │  │
│  │  • loadSessionStats() - Fetch statistics                 │  │
│  │  • displayFilteredSessions() - Render table              │  │
│  │  • filterSessionsByRole() - Switch role view             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  User Actions                                             │  │
│  │  • searchSessions(query) - Client-side filter            │  │
│  │  • sortSessionsByColumn(field) - Client-side sort        │  │
│  │  • loadFilteredSessionsPage(page) - Pagination           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Toggle Actions (API calls)                              │  │
│  │  • toggleSessionNotification(id, enable)                 │  │
│  │  • toggleSessionAlarm(id, enable)                        │  │
│  │  • toggleSessionFeatured(id, enable)                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↕ DOM Manipulation
┌─────────────────────────────────────────────────────────────────┐
│                        UI LAYER (HTML)                           │
│                profile-pages/student-profile.html                │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  #sessions-panel (Container)                              │  │
│  │    │                                                       │  │
│  │    ├─ Stats Cards                                         │  │
│  │    │   • #student-session-stat-total                      │  │
│  │    │   • #student-session-stat-completed                  │  │
│  │    │   • #student-session-stat-hours                      │  │
│  │    │   • #student-session-stat-upcoming                   │  │
│  │    │                                                       │  │
│  │    ├─ Search Bar                                          │  │
│  │    │   • <input oninput="searchSessions(this.value)">     │  │
│  │    │                                                       │  │
│  │    ├─ Role Filter Buttons                                 │  │
│  │    │   • [All] [As Tutor] [As Student] [As Parent]       │  │
│  │    │   • onclick="filterSessionsByRole('tutor', event)"   │  │
│  │    │                                                       │  │
│  │    └─ Sessions Table Container                            │  │
│  │        • #sessions-table-container (Dynamic content)      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## State Machine Diagram

```
┌─────────────────┐
│  INITIAL STATE  │  (Panel closed)
└────────┬────────┘
         │ User clicks "My Sessions"
         │ panelSwitch event fires
         ↓
┌─────────────────┐
│    LOADING      │  Shows spinner
│  ⏳ Loading...  │  Fetching from API
└────────┬────────┘
         │ API response received
         │ allSessionsData populated
         ↓
┌─────────────────────────────────────────┐
│         LOADED (Default View)            │
│  Current Filter: 'student'              │
│  Table: Tutor | Course | Date | ...     │
│  Stats: 📊 15  ✅ 8  ⏰ 12.5  🔔 5      │
└─────────┬───────────────────────────────┘
          │
          ├─────────────────┬──────────────┬──────────────┐
          │                 │              │              │
     [Filter by Role]  [Search]       [Sort]        [Toggle]
          │                 │              │              │
          ↓                 ↓              ↓              ↓
┌──────────────────┐ ┌────────────┐ ┌───────────┐ ┌──────────┐
│  ROLE FILTERED   │ │  SEARCHED  │ │  SORTED   │ │ TOGGLING │
│  • All           │ │  Query:    │ │  By: date │ │  API     │
│  • Tutor         │ │  "math"    │ │  Order:   │ │  Call    │
│  • Student       │ │  Results:  │ │  asc/desc │ │  ...     │
│  • Parent        │ │  5 of 15   │ │           │ │          │
└──────────────────┘ └────────────┘ └───────────┘ └──────────┘
          │                 │              │              │
          │                 │              │              ↓
          │                 │              │         ┌──────────┐
          │                 │              │         │ RELOADING│
          │                 │              │         │ (refresh)│
          │                 │              │         └──────────┘
          │                 │              │              │
          └─────────────────┴──────────────┴──────────────┘
                            │
                            ↓
                    ┌───────────────┐
                    │   DISPLAYED   │
                    │  (New State)  │
                    └───────────────┘
```

## Component Interaction Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                       USER INTERACTIONS                           │
└──────────────────────────────────────────────────────────────────┘
     │          │           │           │           │          │
     │          │           │           │           │          │
  [Open]   [Filter]    [Search]    [Sort]     [Toggle]   [Paginate]
     │          │           │           │           │          │
     ↓          ↓           ↓           ↓           ↓          ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FUNCTION LAYER                                │
│  loadSessions  filterSessions  searchSessions  sortSessions      │
│                          toggleNotification                       │
└─────────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────────┐
│                    STATE LAYER                                   │
│  allSessionsData | sessionStats | currentRoleFilter              │
│  sessionCurrentPage | filteredSessionsCache                      │
└─────────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────────┐
│                    API LAYER                                     │
│  GET /api/student/my-sessions                                    │
│  PATCH /api/student/sessions/{id}/toggle-*                       │
└─────────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE (PostgreSQL)                         │
│  sessions table | enrolled_students table                        │
└─────────────────────────────────────────────────────────────────┘
```

## Namespace Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        WINDOW OBJECT                             │
│                                                                  │
│  window.StudentSessionsPanel = {                                │
│      loadSessions,                                              │
│      loadSessionStats,                                          │
│      searchSessions,                                            │
│      filterSessionsByRole,                                      │
│      loadFilteredSessionsPage,                                  │
│      toggleSessionNotification,                                 │
│      toggleSessionAlarm,                                        │
│      toggleSessionFeatured,                                     │
│      sortSessionsByColumn,                                      │
│      viewSession                                                │
│  }                                                              │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Also exported directly for onclick handlers:            │  │
│  │  window.loadSessions                                     │  │
│  │  window.searchSessions                                   │  │
│  │  window.filterSessionsByRole                             │  │
│  │  window.toggleSessionNotification                        │  │
│  │  window.toggleSessionAlarm                               │  │
│  │  window.sortSessionsByColumn                             │  │
│  │  window.viewSession                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
User action (e.g., toggle notification)
    │
    ↓
toggleSessionNotification(123, true)
    │
    ├─ Check token
    │   └─ No token? → alert("Please log in") → STOP
    │
    ├─ Try API call
    │   │
    │   ├─ Network error → catch block
    │   │   └─ console.error("Error:", error)
    │   │   └─ alert("Failed to update setting")
    │   │
    │   ├─ !response.ok → throw new Error
    │   │   └─ catch block
    │   │   └─ alert("Failed to update setting")
    │   │
    │   └─ Success (200 OK)
    │       ├─ loadSessionsByRole() → Reload data
    │       └─ console.log("✅ Success")
    │
    └─ User sees updated UI
```

---

This architecture provides:
- ✅ Separation of concerns
- ✅ Single source of truth (state variables)
- ✅ Robust error handling
- ✅ Efficient data flow
- ✅ Clear interaction patterns
- ✅ Namespace isolation
- ✅ Extensible design
